---
category: ui
---

`react-icons` のアイコンコンポーネントを `icon` prop に渡して表示できるアイコンボタンです。44×44px の円形クリック領域を持ち、アイコン色は `--color-text` を継承します。`IconButton` を横並びで使用する場合は 4px 以上の余白を設けてください。`label` には用途を表すアクセシブルな名前を指定します。

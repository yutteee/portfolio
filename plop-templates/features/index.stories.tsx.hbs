import type { Meta, StoryObj } from "@storybook/react";
import { {{pascalCase name}}Presenter } from "./presenter";

const meta: Meta<typeof {{pascalCase name}}Presenter> = {
  title: "features/{{pascalCase name}}",
  component: {{pascalCase name}}Presenter,
  tags: ["autodocs"],
  parameters: {
    docs: {
      description: {
        component: "{{pascalCase name}}Presenterの説明をここに記載してください。",
      },
    },
  },
};
export default meta;

type Story = StoryObj<typeof {{pascalCase name}}Presenter>;

export const Default: Story = {}; 
---
import "destyle.css";
import Header from "../components/Header/index.astro";
import Footer from "../components/Footer.astro";
import "../styles/global.css";
const { title, description } = Astro.props;
import { SEO } from "astro-seo";
---

<html lang="ja">
  <head>
    <link rel="icon" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <SEO
      charset="UTF-8"
      title={`${title} | yutteee-portfolio`}
      description={description}
      openGraph={{
        basic: {
          title: `${title} | yutteee-portfolio`,
          type: "article",
          image: "/portfolio.png",
        },
        image: {
          alt: "中村優作のポートフォリオ",
        },
      }}
      twitter={{
        card: "summary_large_image",
        site: "@yutteee-portfolio",
        creator: "@yutteee",
      }}
      extend={{
        link: [{ rel: "icon", href: "/favicon.svg" }],
        meta: [
          { name: "twitter:image", content: "/portfolio.png" },
          { name: "twitter:image:alt", content: "中村優作のポートフォリオ" },
          { name: "twitter:title", content: `${title} | yutteee-portfolio` },
          { name: "twitter:description", content: description },
        ],
      }}
    />
  </head>
  <body>
    <Header currentPage={title} />
    <slot />
    <Footer />
  </body>
</html>

---
import { FiArrowRight } from "react-icons/fi";
import { ProductItem } from "../../ui/ProductItem";
import { getCollection } from "astro:content";
import { Button } from "../../ui/Button";

const productsCollection = (await getCollection("products")).sort((a, b) =>
  b.data.period.localeCompare(a.data.period),
);
---

<section class="container">
  <div class="titles">
    <h2 class="title">プロダクト</h2>
    <Button href="/products/" aria-label="プロダクト一覧" endIcon={FiArrowRight}>
      一覧
    </Button>
  </div>
  <div class="products">
    {
      productsCollection
        .slice(0, 2)
        .map((product, index) => (
          <ProductItem
            index={index + 1}
            image={product.data.image.url}
            title={product.data.title}
            description={product.data.description}
            href={`/products/${product.id}`}
            alt={product.data.image.alt}
          />
        ))
    }
  </div>
</section>

<style>
  .container {
    display: flex;
    flex-direction: column;
    gap: var(--space-2xl);

    .titles {
      display: flex;
      flex-direction: row;
      flex-wrap: wrap;
      align-items: end;
      gap: var(--space-m);

      .title {
        font-size: var(--step-5);
        font-weight: 900;
        line-height: 1;
      }
    }

    .products {
      display: flex;
      gap: var(--space-xl);
      width: 100%;

      @media screen and (max-width: 1024px) /* sync with --bp-md */ {
        flex-direction: column;
      }
    }
  }
</style>

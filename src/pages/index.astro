---
import BaseLayout from "../layouts/BaseLayout.astro";
const pageTitle = "Home Page";
---

<BaseLayout pageTitle={pageTitle}>
  <div class="container">
    <div class="pc-wrapper">
      <div class="pc">
        <div class="monitor">
          <img src="/blog-placeholder-5.jpg" width="300" height="187.5" id="screen" class="screen">
          <img src="/blog-placeholder-4.jpg" width="300" height="187.5" id="screen" class="screen">
          <img src="/blog-placeholder-3.jpg" width="300" height="187.5" id="screen2" class="screen">
          <img src="/blog-placeholder-2.jpg" width="300" height="187.5" id="screen1" class="screen">
          <img src="/blog-placeholder-1.jpg" width="300" height="187.5" id="screen0" class="screen">
        </div>
        <div class="keyboard"></div>
      </div>
    </div>
    <div class="main-wrapper">
      <div class="main" id="main">
        <div class="page">
          <h1 class="title">Hi! I'm Yutteee!</h1>
          <div class="description">
            <p>中村優作といいます。</p>
            <p>名古屋大学工学部化学生命工学科の4年生です。大学ではペプチドの合成をしています。</p>
            <p>名古屋のアプリ開発団体jackで活動しており、web開発を楽しんでいます。特にフロントエンドとデザインが好きです。</p>
            <p>趣味はj-popを聞くこと、お笑いを見ることです。</p>
          </div>
        </div>
        <h2 class="title">History</h2>
        {
          Array.from({ length: 3 }).map((_, index) => (
            <div class="history-item" id={`history-item${index}`}>
              <div class="history-item-description">
                <h3>合同会社KANNON</h3>
                <p>フェアナビというアクセシビリティオーバーレイSaaSの開発をしています。フロントエンドを中心として、デザインの提案などもしながら業務に取り組んでいます。</p>
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</BaseLayout>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  
  const historyItem0 = document.getElementById(`history-item0`);
  const historyItem1 = document.getElementById(`history-item1`);
  const historyItem2 = document.getElementById(`history-item2`);
  const screen0 = document.getElementById('screen0');
  const screen1 = document.getElementById('screen1');
  const screen2 = document.getElementById('screen2');
  
  gsap.to(screen0, {
    scrollTrigger: {
      trigger: historyItem0,
      start: "top center",
      end: "center center",
      scrub: 1,
      toggleActions: "play none none none",
    },
    y: -187.5,
  });

  gsap.to(screen1, {
    scrollTrigger: {
      trigger: historyItem1,
      start: "top center",
      end: "center center",
      scrub: 1,
    },
    y: -187.5,
  });

  gsap.to(screen2, {
    scrollTrigger: {
      trigger: historyItem2,
      start: "top center",
      end: "center center",
      scrub: 1,
    },
    y: -187.5,
  });


</script>

<style>
  .container {
    display: flex;
    justify-content: end;
    gap: 64px;
    width: 100%;
    height: 100%;
  }

  .pc-wrapper {
    position: fixed;
    width: calc((100%  / 12) * 4);
    top: 0;
    left: 0;
    bottom: 0;
    display: flex;
    justify-content: end;
    align-items: center;
    padding-right: calc((100% / 12) / 2);
  }

  .pc {
    display: flex;
    align-items: center;
    flex-direction: column;
    gap: 48px;
  }

  .monitor {
    width: 300px;
    height: 187.5px;
    background-color: var(--color-white);
    border-radius: 16px;
    position: relative;
    overflow: hidden;
  }

  .screen {
    position: absolute;
    top: 0;
    left: 0;
  }

  .keyboard {
    width: 150px;
    border-bottom: 30px solid var(--color-white);
    border-left: 8px solid transparent;
    border-right: 8px solid transparent;
  }

  .main-wrapper {
    width: calc((100% / 12) * 8);
    display: flex;
    justify-content: start;
    align-items: center;
  }

  .main {
    display: flex;
    flex-direction: column;
    max-width: 800px;
    height: fit-content;
    margin-left: calc((100% / 12) / 2);
  }

  .page {
    height: 100svh;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .title {
    font-size: 96px;
    font-weight: 900;
    margin-bottom: 48px;
  }

  .description {
    font-size: 18px;
    line-height: 2;
    font-weight: 900;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .history-item {
    height: 80svh;
    border-top: 1px solid var(--color-black);
    padding: 48px 0;
    position: relative;
  }

  .history-item-description {
    position: sticky;
    top: 48px;
    font-size: 18px;
    line-height: 2;
    font-weight: 900;
    display: flex;
    flex-direction: column;
    gap: 24px;
    height: fit-content;
  }
</style>